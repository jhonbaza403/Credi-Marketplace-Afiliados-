import 'server-only'
import { createHash } from 'node:crypto'
import { createAdminClient } from '@/lib/supabase/admin'
export async function authenticateAgent(request:Request){const auth=request.headers.get('authorization')??'';const token=auth.startsWith('Bearer ')?auth.slice(7).trim():'';if(!token)return null;const hash=createHash('sha256').update(token).digest('hex');const admin=createAdminClient();const {data}=await admin.from('agent_access_tokens').select('id,agent_id,scopes,expires_at,revoked_at,agent:agent_id(id,owner_id,name,status,scopes)').eq('token_hash',hash).maybeSingle();if(!data||data.revoked_at)return null;if(data.expires_at&&new Date(data.expires_at).getTime()<=Date.now())return null;const agent=Array.isArray(data.agent)?data.agent[0]:data.agent;if(!agent||agent.status!=='active')return null;await admin.from('agent_access_tokens').update({last_used_at:new Date().toISOString()}).eq('id',data.id);return {tokenId:data.id,agentId:data.agent_id,ownerId:agent.owner_id,scopes:data.scopes??[]}}
