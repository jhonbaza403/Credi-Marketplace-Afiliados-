import Link from 'next/link'
import type { ReactNode } from 'react'
import { ArrowLeft, ExternalLink } from 'lucide-react'

export function ModuleShell({ eyebrow, title, description, icon, endpoint, children }: { eyebrow: string; title: string; description: string; icon: ReactNode; endpoint?: string; children?: ReactNode }) {
  return <main className="min-h-screen bg-background text-foreground"><div className="mx-auto max-w-6xl px-4 py-8 sm:px-6 lg:px-8 lg:py-12"><Link href="/ecosistema-avanzado" className="inline-flex items-center gap-2 text-sm font-black text-primary hover:underline"><ArrowLeft className="size-4"/>Volver al ecosistema</Link><section className="mt-5 rounded-[2rem] border border-border bg-card p-7 shadow-sm sm:p-10"><div className="flex items-start gap-4"><div className="flex size-14 shrink-0 items-center justify-center rounded-2xl bg-primary/10 text-primary">{icon}</div><div><p className="text-[10px] font-black uppercase tracking-[.2em] text-primary">{eyebrow}</p><h1 className="mt-1 text-3xl font-black tracking-tight sm:text-5xl">{title}</h1><p className="mt-3 max-w-3xl text-sm leading-7 text-muted-foreground sm:text-base">{description}</p>{endpoint ? <a href={endpoint} target="_blank" rel="noreferrer" className="mt-4 inline-flex items-center gap-2 rounded-xl border border-border px-3 py-2 text-xs font-black hover:bg-muted">API / endpoint <ExternalLink className="size-3.5"/></a> : null}</div></div></section>{children ? <section className="mt-6 rounded-3xl border border-border bg-card p-6 shadow-sm sm:p-8">{children}</section> : null}</div></main>
}
